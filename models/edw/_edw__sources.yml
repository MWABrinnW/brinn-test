version: 2

sources:
  - name: raw
    schema: pms
    description: "enterprise data warehouse"
    database: "{{ var('db_raw_src', env_var('DBT_DB_raw_SRC', 'raw')) }}"
    tables:
      - name: accounts
        description: "replicated accounts from snowflake/odw.enterprise.accounts"
      - name: holdings
        description: "replicated holdings from snowflake/odw.enterprise.holdings"