version: 2

sources:
  - name: xcm
    database: "{{ var('db_raw_src', env_var('DBT_DB_RAW_SRC', 'raw')) }}"
    description: ""
    schema: xcm
    tables:
      - name: client_detail
        columns:
          - name: _pk
          - name: json
          - name: _created_at
      - name: task_detail
        columns:
          - name: _pk
          - name: json
          - name: _created_at
