version: 2

sources:
  - name: xcm
    database: "{{ var('db_datalake_src', env_var('DBT_DB_DATALAKE_SRC', 'datalake')) }}" 
    description: ""
    schema: xcm
    tables:
      - name: client_detail
        columns:
          - name: _pk
          - name: json
          - name: _created_at
      - name: task_detail
        columns:
          - name: _pk
          - name: json
          - name: _created_at
