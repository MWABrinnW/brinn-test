version: 2

sources:
  - name: ring_central
    description: ""
    database: "{{ var('db_raw_src', env_var('DBT_DB_RAW_SRC', 'raw')) }}"
    tables:
      - name: sms
        description: ""
        columns:
          - name: JSON
            description: ''
            data_type: VARIANT
