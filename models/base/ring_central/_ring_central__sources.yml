version: 2

sources:
  - name: ring_central
    description: ""
    database: "{{ var('db_datalake_src', env_var('DBT_DB_DATALAKE_SRC', 'datalake')) }}"
    tables:
      - name: sms
        description: ""
        columns:
          - name: JSON
            description: ''
            data_type: VARIANT
