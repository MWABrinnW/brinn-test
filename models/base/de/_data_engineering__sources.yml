version: 2

sources:
  - name: de
    description: "Data engineering schema"
    database: "{{ var('db_raw_src', env_var('DBT_DB_RAW_SRC', 'raw')) }}"
    schema: de
    tables:
      - name: db_grants
      - name: db_roles
      - name: db_users
