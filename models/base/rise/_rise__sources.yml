version: 2

sources:
  - name: rise
    description: "Rise Training application data"
    database: "{{ var('db_datalake_src', env_var('DBT_DB_DATALAKE_SRC', 'datalake')) }}"
    tags: ['rise']
    loaded_at_field: _created_at
    tables:
      - name: api_data
        description: "JSON dump of data from Rise API"
