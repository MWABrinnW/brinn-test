version: 2

sources:
  - name: morningstar
    description: ""
    database: "{{ var('db_raw_src', env_var('DBT_DB_RAW_SRC', 'raw')) }}"
    schema: morningstar
    tables:
      - name: fund
        description: ""