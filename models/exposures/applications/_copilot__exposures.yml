version: 2

exposures:

  - name: copilot_sod
    label: Copilot start of day accounts and positions
    type: application
    maturity: high
    tags: ["copilot", "options"]
    url: ""
    description: >
      The SOD process for uploading accounts and tax lot level positionst to copilot.

      The SOD process generates csv files using the accounts and positions models.
      And then uploads using the copilot API.

    depends_on:
      - ref('flyer__sod_accounts')
      - ref('flyer__sod_positions')
      - ref('flyer__sod_positions_exceptions')

    owner:
      name: Engineering
      email: engineering@marinerwealth.com
