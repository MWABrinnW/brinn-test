version: 2

seeds:
  - name: custodians
    description: List of custodians by firm source.
    docs:
      show: true
    config:
      enabled: true
      tags: ['custodial', 'custodian']
      grants:
        select: "{{ ['db_edw_general_mwa_r'] if target.name in ['prod','test'] else []}}"