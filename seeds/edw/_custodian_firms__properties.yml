version: 2

seeds:
  - name: custodian_firms
    description: Mapping file used for custodian firm label rollups.
    docs:
      show: true
    config:
      enabled: true
      tags: ['custodial', 'custodian']
      grants:
        select: "{{ ['db_edw_general_mwa_r'] if target.name in ['prod', 'test', 'ci'] else []}}"